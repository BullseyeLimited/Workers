app = "fan-card-narrative-writer"
primary_region = "fra"

[build]
  dockerfile = "../../base/Dockerfile"
  context    = "../.."

[processes]
  app = "python -m workers.fan_card_narrative_writer.main"

[deploy]
  min_machines_running = 1
