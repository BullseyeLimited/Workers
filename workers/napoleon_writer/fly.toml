app = "napoleon-writer-worker"
primary_region = "fra"

[build]
  dockerfile = "../../base/Dockerfile"
  context    = "../.."

[processes]
  app = "python -m workers.napoleon_writer.main"

[deploy]
  min_machines_running = 1
