app = "napoleon-worker"
primary_region = "fra"

[build]
  dockerfile = "../../base/Dockerfile"
  context    = "../.."

[processes]
  app = "python workers/napoleon/main.py"

[deploy]
  min_machines_running = 1
