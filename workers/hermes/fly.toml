app = "hermes-worker"
primary_region = "fra"

[build]
  dockerfile = "../../base/Dockerfile"
  context    = "../.."

[processes]
  app = "python -m workers.hermes.main"

[deploy]
  min_machines_running = 1
