app = "iris-worker"
primary_region = "fra"

[env]
  CONTENT_PACK_ENABLED = "true"
  IRIS_CONTROL_ENABLED = "true"

[build]
  dockerfile = "../../base/Dockerfile"
  context    = "../.."

[processes]
  app = "python -m workers.iris.main"

[deploy]
  min_machines_running = 1
